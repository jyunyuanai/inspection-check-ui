# 工程抽查 UI 雲端部署說明

## 第一版雲端限制

- 雲端版只支援 `.docx` 抽查紀錄表樣本。
- `.doc` 舊格式需要 Windows + Microsoft Word COM，Streamlit Cloud 通常無法支援。
- 每個瀏覽器 session 會使用獨立暫存資料夾，避免多人互相覆蓋。
- 暫存資料只供本次操作使用，產出的 Word 請直接下載保存。

## 本機測試密碼

建立：

```text
.streamlit/secrets.toml
```

內容：

```toml
APP_PASSWORD = "請換成你的公司共用密碼"
```

這個檔案已被 `.gitignore` 排除，不要上傳到 GitHub。

## 上傳 GitHub 前

建議只上傳：

- `inspection_check.py`
- `requirements.txt`
- `.gitignore`
- `.streamlit/config.toml`
- `.streamlit/secrets.toml.example`
- `工程抽查照片樣板/` 裡的固定空白樣板

不要上傳：

- `.streamlit/secrets.toml`
- `.streamlit_runtime/`
- `UI匯入照片暫存/`
- `輸出/`
- `抽查紀錄表樣板/`
- `*.sqlite`
- `輸入記憶.json`

## Streamlit Cloud 部署

1. 建立 GitHub 私有 repo。
2. 把專案檔案推到 repo。
3. 到 Streamlit Cloud 建立 app。
4. 選擇 repo、branch，入口檔案填：

```text
inspection_check.py
```

5. 在 Streamlit Cloud 的 Secrets 貼上：

```toml
APP_PASSWORD = "請換成你的公司共用密碼"
```

6. 部署完成後，把網址和共用密碼給同事。
